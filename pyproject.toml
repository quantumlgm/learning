[project]
name = "learning"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx[cli]>=0.28.1",
]

[tool.pyright]
typeCheckingMode = "strict"
